/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "files.edgestore.dev",
            "keny.cloud"
        ]
    }
}

module.exports = nextConfig
